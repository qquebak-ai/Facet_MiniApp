-- Адрес кошелька в профиле.
-- Выполнить один раз в Supabase → SQL Editor.
--
-- Нужен боту: показать баланс и собрать ссылку на продажу он может
-- только зная, чей кошелёк. Приложение записывает адрес при подключении
-- через TonConnect, в переписке его можно задать руками.
--
-- Ключей здесь нет и быть не может — только публичный адрес, тот же,
-- что виден в любом обозревателе цепочки.
alter table public.profiles
  add column if not exists wallet_address text;
