-- Обложка токена: ею подкладывается карточка «в центре внимания».
-- Ссылка на файл в хранилище avatars, куда его кладёт само приложение
-- при запуске. Поле необязательное: у токенов без обложки остаётся
-- прежний фон из усреднённого цвета логотипа.
alter table public.tokens add column if not exists banner_url text;
