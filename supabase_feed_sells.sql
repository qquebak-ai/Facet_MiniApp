-- Сколько из суточных сделок были продажами. По этому числу видно
-- ловушку: покупки идут, продаж нет вовсе. Обход умеет писать и без
-- этой колонки, но тогда приложение не может перепроверить строки,
-- собранные раньше.
alter table public.feed_cache add column if not exists sells24 integer;
